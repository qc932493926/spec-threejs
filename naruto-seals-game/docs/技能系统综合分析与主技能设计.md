# Three.js 手势游戏技能系统综合分析与主技能设计

> **生成日期**: 2026-02-05
> **分析基础**: D:\ai_learn\my_git_threejs\docs 游戏案例
> **已安装技能**: 12 个核心技能

---

## 📋 目录

1. [已安装技能功能分析](#已安装技能功能分析)
2. [技能协同关系图](#技能协同关系图)
3. [游戏案例技能映射](#游戏案例技能映射)
4. [主技能设计方案](#主技能设计方案)
5. [实施建议](#实施建议)

---

## 一、已安装技能功能分析

### 🎮 核心技能层 (Core Skills Layer)

#### 1. **threejs-game**
**功能定位**: Three.js 游戏开发基础框架

**核心能力**:
- 3D 场景设置（Scene, Camera, Renderer）
- 游戏循环系统（requestAnimationFrame）
- 玩家控制器（第一/三人称）
- 碰撞检测（Raycasting）
- 物理系统（重力、跳跃）
- 交互对象管理
- 性能优化（对象池、LOD）

**适用场景**:
- ✅ 水果忍者游戏的 3D 场景搭建
- ✅ AR 射击游戏的核心引擎
- ✅ 圣诞树项目的基础架构
- ✅ JARVIS HUD 的 3D 地球渲染

**关键输出**:
- 游戏循环模板
- 控制器系统
- 碰撞检测算法

---

#### 2. **hand-gesture-recognition**
**功能定位**: 手势识别与交互逻辑

**核心能力**:
- MediaPipe 手势识别集成
- 手部关键点追踪（21个点）
- 手势分类（Open_Palm, Closed_Fist, Pinch等）
- 双手检测与区分
- 手势置信度过滤
- 手部坐标映射到3D空间

**适用场景**:
- ✅ 水果忍者的切水果手势
- ✅ AR射击的瞄准与射击手势
- ✅ 圣诞树的手势控制旋转和散开
- ✅ JARVIS HUD的捏合操作

**关键输出**:
- 手势识别器配置
- 手势→游戏事件映射逻辑
- 实时坐标转换算法

---

### 🎨 渲染增强层 (Rendering Enhancement Layer)

#### 3. **threejs-shaders**
**功能定位**: Three.js 自定义着色器开发

**核心能力**:
- GLSL 着色器编写（Vertex + Fragment）
- 自定义材质创建（shaderMaterial）
- Uniform 变量管理
- 顶点属性操作
- 纹理采样与混合
- 性能优化技巧

**适用场景**:
- ✅ 圣诞树粒子的自定义材质
- ✅ JARVIS 全息效果的发光材质
- ✅ 水果忍者的刀光效果

---

#### 4. **r3f-shaders**
**功能定位**: React Three Fiber 环境下的着色器开发

**核心能力**:
- React 声明式着色器组件
- 与 R3F Hooks 集成（useFrame, useThree）
- 着色器动画系统
- 状态驱动的材质变化
- 性能优化的 useMemo 缓存

**适用场景**:
- ✅ 圣诞树项目（已使用R3F）
- ✅ 需要 React 生态的项目

---

#### 5. **threejs-animation**
**功能定位**: Three.js 动画系统

**核心能力**:
- AnimationMixer 动画混合器
- KeyframeTrack 关键帧动画
- 骨骼动画（Skeletal Animation）
- 变形目标动画（Morph Targets）
- 动画状态机
- Tween 补间动画

**适用场景**:
- ✅ 3D 模型动画（GLTF加载后的角色动画）
- ✅ 相机动画过渡
- ✅ UI元素动画

---

### 🌟 特效与视觉层 (Effects & Visual Layer)

#### 6. **particle-systems**
**功能定位**: 粒子系统设计与实现

**核心能力**:
- 大规模粒子管理（BufferGeometry）
- 粒子生命周期系统
- 粒子发射器设计
- 力场系统（重力、风力）
- 粒子碰撞检测
- GPU 加速（InstancedMesh）

**适用场景**:
- ✅ 圣诞树的 15,000个叶片粒子
- ✅ 水果忍者的切水果爆炸特效
- ✅ AR 射击的爆炸碎片
- ✅ JARVIS 的环绕粒子系统

---

#### 7. **web-audio-api**
**功能定位**: 实时音效系统

**核心能力**:
- Web Audio API 使用
- Oscillator 音调合成
- 程序化音效生成
- 音效缓存与管理
- 空间音效（3D Audio）
- TTS 语音合成

**适用场景**:
- ✅ 水果忍者的切水果音效
- ✅ AR 射击的枪声与命中音效
- ✅ JARVIS 的机械音效和 TTS

**关键输出**:
- 音效生成函数库
- 音效触发时机

---

### 🏗 开发工具层 (Development Tools Layer)

#### 8. **vercel-react-best-practices**
**功能定位**: React 开发最佳实践

**核心能力**:
- React 18+ 特性使用
- Hooks 最佳实践
- 性能优化技巧（useMemo, useCallback）
- 状态管理模式
- 组件设计原则
- TypeScript 集成

**适用场景**:
- ✅ 圣诞树项目
- ✅ AR 射击游戏
- ✅ JARVIS HUD 项目

---

#### 9. **webgl**
**功能定位**: WebGL 底层技术

**核心能力**:
- WebGL Context 管理
- 缓冲区操作
- 纹理管理
- 帧缓冲对象（FBO）
- 性能调试
- 浏览器兼容性

**适用场景**:
- ✅ 低层次性能优化
- ✅ 自定义渲染管线

---

#### 10. **vite**
**功能定位**: 现代化构建工具

**核心能力**:
- 快速冷启动
- 热模块替换（HMR）
- 插件系统
- 资源处理（图片、模型）
- 生产优化构建
- TypeScript 支持

**适用场景**:
- ✅ 所有项目的开发环境搭建

---

### 🎯 设计思维层 (Design Thinking Layer)

#### 11. **game-design-theory**
**功能定位**: 游戏设计理论

**核心能力**:
- 游戏机制设计
- 玩家体验优化
- 反馈循环设计
- 难度曲线设置
- 游戏平衡性
- UI/UX 设计原则

**适用场景**:
- ✅ 游戏玩法设计阶段
- ✅ 交互体验优化

---

#### 12. **brainstorming**
**功能定位**: 创意头脑风暴

**核心能力**:
- 创意生成方法
- 需求分析
- 功能头脑风暴
- 技术方案对比
- 创新思维引导

**适用场景**:
- ✅ 游戏概念阶段
- ✅ 功能扩展讨论

---

## 二、技能协同关系图

```
                    ┌─────────────────────────────────┐
                    │     brainstorming               │
                    │   (创意与需求分析)               │
                    └──────────────┬──────────────────┘
                                   │
                                   ▼
                    ┌─────────────────────────────────┐
                    │   game-design-theory            │
                    │   (游戏设计理论)                 │
                    └──────────────┬──────────────────┘
                                   │
        ┌──────────────────────────┼──────────────────────────┐
        │                          │                          │
        ▼                          ▼                          ▼
┌────────────────┐      ┌──────────────────┐      ┌────────────────────┐
│  vercel-react  │      │   threejs-game   │      │hand-gesture-recog  │
│best-practices  │◄────►│   (核心引擎)      │◄────►│  (手势识别)         │
│  (React架构)    │      └────────┬─────────┘      └──────────┬─────────┘
└────────┬───────┘               │                           │
         │                       │                           │
         │        ┌──────────────┼──────────────┐           │
         │        │              │              │           │
         ▼        ▼              ▼              ▼           ▼
    ┌─────────┐ ┌───────┐  ┌────────┐   ┌─────────┐  ┌──────────┐
    │  vite   │ │webgl  │  │shaders │   │animation│  │particles │
    │(构建工具)│ │(底层) │  │(材质)   │   │(动画)   │  │(粒子)     │
    └─────────┘ └───────┘  └────┬───┘   └─────────┘  └──────────┘
                                 │
                                 ▼
                            ┌──────────┐
                            │r3f-shaders│
                            │(R3F材质)  │
                            └──────────┘
                                 │
                                 ▼
                          ┌──────────────┐
                          │web-audio-api │
                          │  (音效系统)   │
                          └──────────────┘
```

**关系说明**:
- **顶层**: 创意与设计驱动
- **核心层**: 三大支柱（React + Three.js + 手势识别）
- **增强层**: 渲染、动画、粒子、音效
- **基础层**: 构建工具与底层技术

---

## 三、游戏案例技能映射

### 🎄 案例 1: 3D 圣诞树项目

**核心技能组合**:
```
brainstorming (创意)
    → game-design-theory (设计)
    → vercel-react-best-practices (架构)
    → threejs-game (引擎)
    → hand-gesture-recognition (手势)
    → r3f-shaders (自定义材质)
    → threejs-animation (动画)
    → particle-systems (15K粒子)
    → web-audio-api (音效)
    → vite (构建)
```

**技能应用详情**:
| 技能 | 应用场景 | 具体实现 |
|------|---------|----------|
| hand-gesture-recognition | 手势控制 | Open_Palm→CHAOS, Closed_Fist→FORMED |
| particle-systems | 树叶粒子 | 15,000个粒子，BufferGeometry优化 |
| r3f-shaders | 自定义材质 | FoliageMaterial，混合Blending |
| threejs-animation | 粒子动画 | useFrame + MathUtils.damp 平滑过渡 |
| web-audio-api | （未实现） | 可添加手势切换音效 |

---

### 🥷 案例 2: 水果忍者游戏

**核心技能组合**:
```
brainstorming
    → game-design-theory
    → threejs-game (3D场景)
    → hand-gesture-recognition (食指轨迹)
    → threejs-shaders (刀光效果)
    → particle-systems (水果爆炸)
    → web-audio-api (切水果音效)
```

**技能应用详情**:
| 技能 | 应用场景 | 具体实现 |
|------|---------|----------|
| hand-gesture-recognition | 刀光生成 | 追踪食指位置，生成轨迹 |
| threejs-game | 碰撞检测 | Raycasting检测刀光与水果碰撞 |
| particle-systems | 爆炸特效 | 水果切开后的粒子爆炸 |
| web-audio-api | 音效 | Oscillator生成切水果音效 |
| threejs-shaders | 刀光 | AdditiveBlending发光效果 |

---

### 🎯 案例 3: AR 手势射击游戏

**核心技能组合**:
```
brainstorming
    → game-design-theory (Game Juice设计)
    → vercel-react-best-practices
    → threejs-game (游戏循环)
    → hand-gesture-recognition (食指+拇指手势)
    → particle-systems (爆炸碎片)
    → threejs-animation (3D浮动文字)
    → web-audio-api (射击+命中音效)
```

**技能应用详情**:
| 技能 | 应用场景 | 具体实现 |
|------|---------|----------|
| hand-gesture-recognition | 瞄准+射击 | 食指位置=准心，拇指捏合=扳机 |
| game-design-theory | Game Juice | 屏幕震动+粒子爆炸+音调变化 |
| particle-systems | 爆炸特效 | 10-20个碎片，受重力影响 |
| threejs-animation | 浮动文字 | "+100"分数向上漂浮消失 |
| web-audio-api | 动态音效 | Combo数越高，音调越高 |

---

### 🤖 案例 4: JARVIS 全息 HUD

**核心技能组合**:
```
brainstorming
    → game-design-theory (UI/UX设计)
    → vercel-react-best-practices (React架构)
    → threejs-game (3D地球)
    → hand-gesture-recognition (左手展开+右手旋转)
    → threejs-shaders (全息材质)
    → threejs-animation (地球旋转+地形切换)
    → particle-systems (卫星粒子)
    → web-audio-api (机械音效+TTS)
    → vite (项目构建)
```

**技能应用详情**:
| 技能 | 应用场景 | 具体实现 |
|------|---------|----------|
| hand-gesture-recognition | 双手手势 | 左手展开度控制，右手旋转控制 |
| threejs-shaders | 全息效果 | meshPhongMaterial + AdditiveBlending |
| threejs-animation | 地形切换 | exp>0.55触发战术地形，平滑过渡 |
| particle-systems | 卫星粒子 | Points + PointMaterial |
| web-audio-api | 交互音效 | playBlip, playLock, playServo, TTS |

---

## 四、主技能设计方案

### 🚀 主技能名称: **threejs-gesture-game-generator**

**设计理念**:
> 集成 12 个已安装技能的能力，通过用户的自然语言描述，自动生成可交互手势的 Three.js 网页游戏。

---

### 📐 主技能架构

```
threejs-gesture-game-generator
│
├── 1️⃣ 创意理解模块 (brainstorming)
│   ├─ 需求解析
│   ├─ 游戏类型识别
│   ├─ 玩法提取
│   └─ 技术可行性评估
│
├── 2️⃣ 游戏设计模块 (game-design-theory)
│   ├─ 核心机制设计
│   ├─ 反馈循环设计
│   ├─ 难度曲线
│   └─ UI/UX规划
│
├── 3️⃣ 架构生成模块 (vercel-react-best-practices + vite)
│   ├─ 项目结构生成
│   ├─ 依赖配置
│   ├─ TypeScript配置
│   └─ Vite配置
│
├── 4️⃣ 3D引擎模块 (threejs-game + webgl)
│   ├─ Scene/Camera/Renderer设置
│   ├─ 游戏循环
│   ├─ 碰撞检测
│   └─ 性能优化
│
├── 5️⃣ 手势交互模块 (hand-gesture-recognition)
│   ├─ MediaPipe初始化
│   ├─ 手势→游戏事件映射
│   ├─ 坐标转换
│   └─ 置信度过滤
│
├── 6️⃣ 视觉增强模块 (shaders + animation + particles)
│   ├─ 自定义Shader生成
│   ├─ 粒子系统配置
│   ├─ 动画序列
│   └─ 后期效果
│
└── 7️⃣ 音效模块 (web-audio-api)
    ├─ 音效库生成
    ├─ 触发时机配置
    └─ 3D空间音效
```

---

### 💡 主技能核心能力

#### Capability 1: 自然语言游戏生成

**输入示例**:
```
用户: "我想要一个用手势控制的太空射击游戏，
      用食指瞄准，捏合拇指射击，击中敌人会爆炸"
```

**处理流程**:
1. **brainstorming** 解析需求
   - 游戏类型: 射击游戏
   - 核心玩法: 瞄准 + 射击
   - 视觉效果: 爆炸特效

2. **game-design-theory** 设计机制
   - 目标: 击落敌人
   - 反馈: 爆炸 + 音效 + 分数
   - 难度: 敌人速度逐渐增加

3. **架构生成** 创建项目
   ```
   space-shooter/
   ├── src/
   │   ├── App.tsx
   │   ├── components/
   │   │   ├── GameScene.tsx
   │   │   ├── GestureController.tsx
   │   │   └── Enemy.tsx
   │   ├── services/
   │   │   ├── audioService.ts
   │   │   └── gestureService.ts
   │   └── types.ts
   └── vite.config.ts
   ```

4. **引擎代码生成** (threejs-game)
   ```tsx
   // 自动生成游戏循环
   function GameLoop() {
     useFrame((state, delta) => {
       updateEnemies(delta);
       checkCollisions();
       updateScore();
     });
   }
   ```

5. **手势映射** (hand-gesture-recognition)
   ```tsx
   // 自动生成手势识别
   const gestureMapping = {
     indexFinger: (x, y) => setCrosshairPos(x, y),
     pinchGesture: () => fireBullet()
   };
   ```

6. **特效生成** (particle-systems)
   ```tsx
   // 自动生成爆炸粒子
   function createExplosion(position) {
     const particles = new ParticleSystem({
       count: 20,
       emissiveColor: '#FFD700',
       gravity: true
     });
   }
   ```

7. **音效配置** (web-audio-api)
   ```tsx
   // 自动生成音效系统
   const audioEvents = {
     onFire: () => playShootSound(),
     onHit: () => playExplosionSound()
   };
   ```

---

#### Capability 2: 游戏模板库

提供预设模板，快速生成常见游戏类型：

1. **射击游戏模板**
   - 手势: 食指瞄准 + 捏合射击
   - 特效: 子弹轨迹 + 爆炸粒子
   - 音效: 射击声 + 命中声

2. **切水果模板**
   - 手势: 手指轨迹追踪
   - 特效: 刀光 + 水果爆炸
   - 音效: 切割声

3. **旋转控制模板**
   - 手势: 手掌位置 → 旋转速度
   - 特效: 粒子散开/聚合
   - 音效: 状态切换音

4. **AR交互模板**
   - 手势: 双手协同控制
   - 特效: 全息UI + 粒子环绕
   - 音效: 机械音效 + TTS

---

#### Capability 3: 智能代码生成

**自动生成完整项目文件**:

```typescript
// types.ts
interface GameConfig {
  controls: GestureMapping;
  enemies: EnemyConfig[];
  particles: ParticleConfig;
  audio: AudioConfig;
}

// GameScene.tsx
export function GameScene({ config }: { config: GameConfig }) {
  const [score, setScore] = useState(0);
  const [combo, setCombo] = useState(0);

  // 自动注入手势控制
  useGestureControl(config.controls);

  // 自动注入碰撞检测
  useCollisionDetection({
    onHit: (enemy) => {
      createExplosion(enemy.position);
      playHitSound();
      setScore(s => s + 100 * combo);
      setCombo(c => c + 1);
    }
  });

  return (
    <Canvas>
      <Suspense fallback={null}>
        <GameObjects config={config} />
      </Suspense>
    </Canvas>
  );
}
```

---

#### Capability 4: 手势模式库

预定义常见手势交互模式：

| 手势模式 | 识别方式 | 游戏映射 |
|---------|---------|---------|
| **瞄准模式** | 食指位置追踪 | 准心位置 |
| **点击模式** | 拇指+食指捏合 | 射击/选择 |
| **旋转模式** | 手腕XY坐标 | 视角旋转 |
| **缩放模式** | 双手距离 | 物体缩放 |
| **展开模式** | 手指间距 | 粒子散开度 |
| **挥动模式** | 手部速度 | 刀光轨迹 |

---

#### Capability 5: 性能优化预设

**自动应用优化策略**:

```typescript
// 自动生成的优化配置
const optimizationConfig = {
  particles: {
    maxCount: 10000,
    useInstancedMesh: true,
    frustumCulling: true
  },
  rendering: {
    antialias: false,  // 配合后期处理
    pixelRatio: Math.min(window.devicePixelRatio, 2),
    powerPreference: "high-performance"
  },
  physics: {
    useWebWorker: true,  // 物理计算移到Worker
    simpleCollision: true
  }
};
```

---

### 🛠 主技能使用流程

#### 流程图:

```
用户输入游戏描述
    ↓
[brainstorming] 需求解析
    ↓
[game-design-theory] 机制设计
    ↓
[架构生成] 项目结构创建
    ↓
┌───────────────┼───────────────┐
│               │               │
▼               ▼               ▼
[threejs-game] [hand-gesture]  [web-audio]
    │               │               │
    └───────────────┼───────────────┘
                    ↓
          [shaders + particles + animation]
                    ↓
              完整游戏项目
                    ↓
         [vite] 开发服务器启动
```

---

### 📝 主技能接口设计

```typescript
// 主技能 API
interface ThreeJSGestureGameGenerator {
  // 1. 自然语言生成
  generateFromDescription(
    description: string,
    options?: GenerationOptions
  ): Promise<GameProject>;

  // 2. 模板生成
  generateFromTemplate(
    templateName: 'shooting' | 'fruit-ninja' | 'rotation' | 'ar-interaction',
    customizations?: TemplateCustomizations
  ): Promise<GameProject>;

  // 3. 交互式生成
  interactiveGeneration(
    onStep: (step: GenerationStep) => void
  ): Promise<GameProject>;

  // 4. 热更新
  hotReloadFeature(
    projectPath: string,
    feature: FeatureUpdate
  ): Promise<void>;
}

// 生成结果
interface GameProject {
  structure: ProjectStructure;
  files: Map<string, string>;  // 文件路径 → 文件内容
  dependencies: PackageJson;
  readme: string;
  preview: string;  // 预览图片
}
```

---

### 🎯 使用示例

#### 示例 1: 快速生成射击游戏

```typescript
const generator = new ThreeJSGestureGameGenerator();

const project = await generator.generateFromDescription(`
  创建一个太空射击游戏:
  - 用食指控制准心
  - 捏合拇指发射子弹
  - 敌人是彩色霓虹圆柱体
  - 击中后爆炸成粒子
  - 连击数越高，音效越尖锐
`);

// 自动生成完整项目
await project.save('./space-shooter');
await project.runDevServer();
```

**生成结果**:
- ✅ 完整的 Vite + React + Three.js 项目
- ✅ MediaPipe 手势识别已配置
- ✅ 游戏循环和碰撞检测已实现
- ✅ 粒子爆炸系统已集成
- ✅ 程序化音效已配置
- ✅ 开发服务器自动启动

---

#### 示例 2: 使用模板快速开发

```typescript
const project = await generator.generateFromTemplate('fruit-ninja', {
  fruits: ['🍉', '🍎', '🍌', '🥝'],
  gesture: 'swipe',
  difficulty: 'medium',
  visual: {
    background: 'dark',
    particles: 'colorful',
    trailEffect: 'glow'
  }
});
```

---

#### 示例 3: 交互式生成

```typescript
await generator.interactiveGeneration(async (step) => {
  console.log(`步骤 ${step.index}: ${step.description}`);

  if (step.requiresInput) {
    const answer = await promptUser(step.question);
    step.respond(answer);
  }
});

// 控制台输出:
// 步骤 1: 分析游戏类型...
// 步骤 2: 您想要单人还是多人游戏? [答: 单人]
// 步骤 3: 生成3D场景...
// 步骤 4: 配置手势识别...
// 步骤 5: 您希望使用哪种手势? [答: 捏合射击]
// ...
```

---

## 五、实施建议

### 🎯 阶段 1: 基础框架搭建 (Week 1-2)

**目标**: 创建主技能的核心架构

**任务清单**:
- [ ] 创建 `threejs-gesture-game-generator` 技能目录
- [ ] 编写 `SKILL.md` 文档
- [ ] 实现需求解析模块（brainstorming集成）
- [ ] 实现游戏设计模块（game-design-theory集成）
- [ ] 创建代码模板库

**输出**:
- 技能框架代码
- 基础模板（至少2个）

---

### 🚀 阶段 2: 代码生成引擎 (Week 3-4)

**目标**: 实现自动代码生成

**任务清单**:
- [ ] 集成 threejs-game 模板
- [ ] 集成 hand-gesture-recognition 配置生成
- [ ] 实现文件结构生成器
- [ ] 实现依赖配置生成器
- [ ] 添加类型定义生成

**输出**:
- 可运行的代码生成器
- 能生成基础游戏项目

---

### 🎨 阶段 3: 视觉与音效增强 (Week 5-6)

**目标**: 集成视觉和音效系统

**任务清单**:
- [ ] 集成 particle-systems 生成
- [ ] 集成 shaders 模板
- [ ] 集成 animation 系统
- [ ] 集成 web-audio-api 配置
- [ ] 实现效果预览功能

**输出**:
- 完整的特效生成系统
- 音效库生成器

---

### 🧪 阶段 4: 测试与优化 (Week 7-8)

**目标**: 测试和性能优化

**任务清单**:
- [ ] 生成4个示例游戏
- [ ] 性能测试与优化
- [ ] 浏览器兼容性测试
- [ ] 文档完善
- [ ] 使用指南编写

**输出**:
- 稳定的主技能 v1.0
- 完整文档

---

## 六、技术栈总结

### 前端框架
- **React 18+**: UI框架
- **TypeScript**: 类型安全
- **Vite**: 构建工具

### 3D渲染
- **Three.js**: 3D引擎
- **React Three Fiber**: React集成
- **@react-three/drei**: 辅助工具
- **@react-three/postprocessing**: 后期效果

### AI视觉
- **MediaPipe Tasks Vision**: 手势识别
- **getUserMedia API**: 摄像头访问

### 音效
- **Web Audio API**: 实时音效
- **Oscillator**: 音调合成

### 性能优化
- **BufferGeometry**: 高性能几何体
- **InstancedMesh**: 批量渲染
- **Web Worker**: 多线程计算

---

## 七、结论

通过整合 12 个已安装技能，我们构建了一个强大的 **threejs-gesture-game-generator** 主技能系统。

### ✨ 核心价值

1. **快速原型**: 从创意到可玩游戏，5分钟内完成
2. **最佳实践**: 自动应用行业最佳实践
3. **高性能**: 内置性能优化策略
4. **可扩展**: 模块化设计，易于扩展
5. **开发者友好**: 完整的类型定义和文档

### 🎯 适用场景

- ✅ 游戏创意原型验证
- ✅ 教育与学习项目
- ✅ AR/VR 交互演示
- ✅ 商业游戏快速开发
- ✅ 技术展示与演讲

### 🚀 未来扩展方向

1. **多人游戏支持** (WebRTC/WebSocket)
2. **VR/AR 设备支持** (WebXR)
3. **AI对手** (TensorFlow.js)
4. **云端保存** (Firebase/Supabase)
5. **社交分享** (Web Share API)

---

**文档版本**: v1.0
**最后更新**: 2026-02-05
**作者**: Claude Agent System
**授权**: MIT License
